name: configuration
description: A new Flutter module.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Network
  retrofit:
  shared_preferences:
  intl:
  get_it:
  logging:

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.commerce.configuration
    iosBundleIdentifier: com.commerce.configuration
